apiVersion: v1
kind: Pod
metadata:
  finalizers:
    - kubernetes
  labels:
    name: pod-stuck-in-terminating
    exclude-me: "true"
  name: pod-stuck-in-terminating
  namespace: collector-targets
spec:
  containers:
  - name: pod-stuck-in-terminating
    image: busybox
    resources:
      requests:
        cpu: 100m
        memory: 50G