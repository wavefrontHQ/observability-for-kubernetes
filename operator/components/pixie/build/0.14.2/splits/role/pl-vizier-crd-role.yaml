---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app: pl-monitoring
    component: vizier
    vizier-bootstrap: "true"
  name: pl-vizier-crd-role
  namespace: pl
rules:
  - apiGroups:
      - px.dev
    resources:
      - viziers
      - viziers/status
    verbs:
      - get
      - list
      - watch
